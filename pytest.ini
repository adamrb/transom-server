[pytest]
# bare `pytest` (CI) does not put the repo root on sys.path; `python -m pytest` does
pythonpath = .
markers =
    integration: runs real model inference (downloads models; excluded in CI unit job)
